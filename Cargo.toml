[package]
name = "hex3"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[features]
default = []
single-threaded = []  # Disable rayon for cleaner profiling

[profile.checked]
inherits = "release"
opt-level = 3
debug = true
debug-assertions = true

[profile.checked.package."*"]
debug = false
debug-assertions = false

[profile.profiled]
inherits = "release"
opt-level = 3
debug = true

[dependencies]
# Graphics
wgpu = "27"
winit = "0.30"
pollster = "0.4"

# Math
glam = { version = "0.30", features = ["bytemuck", "serde"] }
bytemuck = { version = "1.19", features = ["derive"] }
encase = { version = "0.11", features = ["glam"] }

# Utilities
rand = "0.8"
rand_chacha = "0.3"
rand_distr = "0.4"
noise = "0.9"
ordered-float = "4.2"
log = "0.4"
env_logger = "0.11"
rayon = "1.10"

# Convex hull
qhull-enhanced = "0.1"

# Spatial indexing
kiddo = "4"

# Spherical Voronoi computation (knn-clipping backend). Upstream renamed the
# package; keep the local crate name so this substrate upgrade stays isolated
# from Hex3 naming churn.
s2-voronoi = { package = "voronoi-mesh", git = "https://github.com/PeterKlimk/s2-voronoi", rev = "e8804a639ea3c989e1ce9ea44b4c66c5f2d7e060" }

# CLI and serialization
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bincode = "1"
flate2 = "1"

# PNG output (offscreen sweep renders)
png = "0.17"
